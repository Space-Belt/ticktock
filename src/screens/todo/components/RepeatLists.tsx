import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const RepeatLists = (props: Props) => {
  return (
    <View>
      <Text>RepeatLists</Text>
    </View>
  );
};

export default RepeatLists;

const styles = StyleSheet.create({});
