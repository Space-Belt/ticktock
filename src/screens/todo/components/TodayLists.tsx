import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

type Props = {};

const TodayLists = (props: Props) => {
  return (
    <View>
      <Text>TodayLists</Text>
    </View>
  );
};

export default TodayLists;

const styles = StyleSheet.create({});
